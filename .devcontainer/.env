# PowerShell Development Environment Variables

# Set the PowerShell version you want to use
# Options: "5.x", "7.x", "latest"
# Note: 5.x will default to 7.x in the container for cross-platform compatibility
POWERSHELL_VERSION=7.x

# Git configuration (optional - set these if you want them pre-configured)
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=your.email@example.com

# Additional environment variables for development
WORKSPACE_DIR=/workspace
SCRIPTS_DIR=/workspace/scripts
MODULES_DIR=/workspace/modules
TESTS_DIR=/workspace/tests
